// Ttpe integer or decimal
const integer = 43
const decimal = 43.1
console.log(typeof decimal, typeof integer)

// Notation scientific
const scientific = 5e3
console.log(typeof scientific)

// Infinite y NaN
const infinite = Infinity
const NanNumber = NaN
console.log(typeof infinite, typeof NanNumber)

// Arithmetic operations

// addition, subtraction, multiplication, division
// + - * /

// module, exponentiation
// %, **

// precision
const result = 0.1 + 0.2
console.log(result.toFixed(1))

// Advanced operations
const squareRoot = Math.sqrt(16)
const absoluteValue = Math.abs(-1)
const random = Math.random()